<section class="ve-ba ve-section">
  <div class="ve-page ve-ba__grid">
    <div class="ve-ba__copy">
      <span class="ve-eyebrow">{{ section.settings.eyebrow | default: 'RESULTS' }}</span>
      <h2 class="ve-h2">{{ section.settings.heading }}</h2>
      <p class="ve-lead">{{ section.settings.body }}</p>
      {%- if section.settings.stat_number != blank -%}<div class="ve-ba__stat"><strong>{{ section.settings.stat_number }}</strong><span>{{ section.settings.stat_label }}</span></div>{%- endif -%}
    </div>
    <div class="ve-ba__visual" id="ve-ba-{{ section.id }}">
      <img class="ve-ba__before" src="{{ section.settings.before_url | default: 'https://images.unsplash.com/photo-1559599101-f09722fb4948?w=800&q=80' }}" alt="Before" width="800" height="800" loading="lazy">
      <div class="ve-ba__after-wrap" style="--reveal: 50%">
        <img class="ve-ba__after" src="{{ section.settings.after_url | default: 'https://images.unsplash.com/photo-1556228720-195a672e8a03?w=800&q=80' }}" alt="After" width="800" height="800" loading="lazy">
      </div>
      <div class="ve-ba__handle" aria-hidden="true">⇆</div>
      <input class="ve-ba__range" type="range" min="0" max="100" value="50" aria-label="Reveal slider">
    </div>
  </div>
</section>
<script>(() => { const root = document.getElementById('ve-ba-{{ section.id }}'); if (!root) return; const r = root.querySelector('.ve-ba__range'); const w = root.querySelector('.ve-ba__after-wrap'); const h = root.querySelector('.ve-ba__handle'); r.addEventListener('input', () => { const v = r.value + '%'; w.style.setProperty('--reveal', v); h.style.left = v; }); })();</script>
{% stylesheet %}
.ve-ba__grid { display: grid; grid-template-columns: 1fr 1fr; gap: 3rem; align-items: center; }
.ve-ba__copy { display: grid; gap: 1rem; max-width: 50ch; }
.ve-ba__stat { display: grid; gap: 0.25rem; padding-top: 1rem; border-top: 1px solid rgba(0,0,0,0.1); }
.ve-ba__stat strong { font-family: var(--ve-display); font-size: 2rem; line-height: 1; }
.ve-ba__visual { position: relative; aspect-ratio: 1; border-radius: 8px; overflow: hidden; }
.ve-ba__before, .ve-ba__after { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: cover; }
.ve-ba__after-wrap { position: absolute; inset: 0; clip-path: inset(0 calc(100% - var(--reveal, 50%)) 0 0); overflow: hidden; }
.ve-ba__handle { position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); background: white; padding: 0.5rem 0.75rem; border-radius: 999px; pointer-events: none; box-shadow: 0 4px 16px rgba(0,0,0,0.2); }
.ve-ba__range { position: absolute; inset: 0; width: 100%; height: 100%; opacity: 0; cursor: ew-resize; }
@media (max-width: 880px) { .ve-ba__grid { grid-template-columns: 1fr; } }
{% endstylesheet %}
{% schema %}
{ "name": "Before / after", "tag": "section", "settings": [
  { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "RESULTS" },
  { "type": "text", "id": "heading", "label": "Heading", "default": "Visible difference in 14 days." },
  { "type": "textarea", "id": "body", "label": "Body", "default": "Independent split-test, 32 participants. Drag the slider to compare." },
  { "type": "text", "id": "stat_number", "label": "Stat number", "default": "+47%" },
  { "type": "text", "id": "stat_label", "label": "Stat label", "default": "Shine measured at week 4" },
  { "type": "text", "id": "before_url", "label": "Before image URL" },
  { "type": "text", "id": "after_url", "label": "After image URL" }
], "presets": [{ "name": "Before / after" }]}
{% endschema %}
