{%- comment %} collection.asymmetric-tiles.5-shape — repeating ABCDE rhythm {% endcomment -%}
<section class="ve-feat ve-section" data-section-id="{{ section.id }}">
  <div class="ve-page">
    <header class="ve-feat__head ve-reveal">
      <div>
        <span class="ve-eyebrow">{{ section.settings.eyebrow }}</span>
        <h2 class="ve-h2 ve-italic">{{ section.settings.heading }}</h2>
      </div>
      {%- if section.settings.link_url != blank -%}<a class="ve-link" href="{{ section.settings.link_url }}">{{ section.settings.link_text }}</a>{%- endif -%}
    </header>
    {%- assign coll = section.settings.collection -%}
    {%- assign show_limit = section.settings.products_to_show | default: 10 -%}
    <div class="ve-feat__asym">
      {%- if coll.products.size > 0 -%}
        {%- for product in coll.products limit: show_limit -%}
          {%- assign slot_index = forloop.index0 | modulo: 5 -%}
          {%- case slot_index -%}{%- when 0 -%}{%- assign slot = 'a' -%}{%- when 1 -%}{%- assign slot = 'b' -%}{%- when 2 -%}{%- assign slot = 'c' -%}{%- when 3 -%}{%- assign slot = 'd' -%}{%- else -%}{%- assign slot = 'e' -%}{%- endcase -%}
          <a class="ve-asym__tile ve-asym__tile--{{ slot }} ve-reveal" href="{{ product.url }}">
            {%- if product.featured_image -%}{%- assign atile = product.title | escape -%}{{ product.featured_image | image_url: width: 800 | image_tag: loading: 'lazy', alt: atile }}
            {%- else -%}<div class="ve-asym__ph" style="background:linear-gradient(135deg,var(--ve-sage) 0%,var(--ve-clay) 100%);"></div>{%- endif -%}
            <span class="ve-asym__label">{{ product.title }} · {{ product.price | money_without_trailing_zeros }}</span>
          </a>
        {%- endfor -%}
      {%- else -%}
        {%- for i in (1..5) -%}
          {%- assign slot_index = forloop.index0 | modulo: 5 -%}
          {%- case slot_index -%}{%- when 0 -%}{%- assign slot = 'a' -%}{%- when 1 -%}{%- assign slot = 'b' -%}{%- when 2 -%}{%- assign slot = 'c' -%}{%- when 3 -%}{%- assign slot = 'd' -%}{%- else -%}{%- assign slot = 'e' -%}{%- endcase -%}
          <div class="ve-asym__tile ve-asym__tile--{{ slot }} ve-reveal">
            <img src="https://images.unsplash.com/photo-{{ '1631730486572-226d1f595b68,1626015449731-d586b6e34d2c,1601049541289-9b1b7bbbfe19,1571423023395-c8f9e1a3ad33,1556228720-195a672e8a03' | split: ',' | slice: forloop.index0 | first }}?w=800&q=80" alt="" loading="lazy" width="800" height="800">
            <span class="ve-asym__label">Botanical wash · $28</span>
          </div>
        {%- endfor -%}
      {%- endif -%}
    </div>
  </div>
</section>

{% stylesheet %}
.ve-feat__head { display: flex; justify-content: space-between; align-items: end; gap: var(--ve-space-4); margin-bottom: var(--ve-space-6); }
.ve-feat__head h2 { margin-top: var(--ve-space-2); }
.ve-feat__asym { display: grid; grid-template-columns: repeat(6, 1fr); grid-auto-rows: 220px; gap: var(--ve-space-3); }
.ve-asym__tile { position: relative; overflow: hidden; border-radius: var(--ve-radius); display: block; background: var(--ve-bg-2); }
.ve-asym__tile img { width: 100%; height: 100%; object-fit: cover; transition: transform var(--ve-dur-slow) var(--ve-ease); }
.ve-asym__tile:hover img { transform: scale(1.03); }
.ve-asym__ph { width: 100%; height: 100%; }
.ve-asym__tile--a { grid-column: span 2; }
.ve-asym__tile--b { grid-column: span 2; grid-row: span 2; }
.ve-asym__tile--c { grid-column: span 2; }
.ve-asym__tile--d { grid-column: span 3; }
.ve-asym__tile--e { grid-column: span 1; grid-row: span 2; }
.ve-asym__label { position: absolute; left: var(--ve-space-3); bottom: var(--ve-space-3); background: rgba(251, 246, 236, 0.94); padding: 0.3rem 0.7rem; border-radius: var(--ve-radius); font-size: var(--ve-fs-xs); color: var(--ve-ink); backdrop-filter: blur(8px); font-weight: 500; }
@media (max-width: 880px) { .ve-feat__asym { grid-template-columns: repeat(2, 1fr); grid-auto-rows: 200px; }
  .ve-asym__tile--a, .ve-asym__tile--b, .ve-asym__tile--c, .ve-asym__tile--d, .ve-asym__tile--e { grid-column: auto; grid-row: auto; }
}
{% endstylesheet %}
{% schema %}
{ "name": "Featured (asymmetric)", "tag": "section", "settings": [
  { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "The range" },
  { "type": "text", "id": "heading", "label": "Heading", "default": "Everyday rituals." },
  { "type": "collection", "id": "collection", "label": "Collection" },
  { "type": "range", "id": "products_to_show", "label": "Products", "min": 5, "max": 15, "step": 1, "default": 10 },
  { "type": "text", "id": "link_text", "label": "Link text", "default": "Explore the range" },
  { "type": "url", "id": "link_url", "label": "Link" }
], "presets": [{ "name": "Featured (asymmetric)" }] }
{% endschema %}
